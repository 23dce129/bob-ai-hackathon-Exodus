# --- Web framework ---
# Pinned: fastapi 0.115.0+ requires Starlette ≥1.6 which has a breaking change
# in Router.__init__. Pin to <0.115.0 until upstream is resolved.
fastapi>=0.111.0,<0.115.0
starlette>=0.37.2,<0.39.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# --- Settings & env ---
python-dotenv>=1.0.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# --- Data & analytics ---
pandas>=2.2.0
pyarrow>=15.0.0
duckdb>=0.10.0
openpyxl>=3.1.0
scipy>=1.13.0
scikit-learn>=1.4.0

# --- IBM watsonx.ai (AI narrative generation) ---
ibm-watsonx-ai>=1.0.0

# --- HTTP client (MCP server → backend calls) ---
httpx>=0.27.0

# --- Testing ---
pytest>=8.2.0
pytest-asyncio>=0.23.0
